# Rendimiento

Documento vivo: se completa con objetivos y mediciones concretas a partir
del Incremento 2 (cuando exista canvas editable con datos reales). Regla
general desde ya: ninguna accion local (crear, mover, editar) debe
depender de la red; persistencia y validacion pesada no deben bloquear la
interaccion principal; nada de loaders de pantalla completa para
operaciones locales normales.
