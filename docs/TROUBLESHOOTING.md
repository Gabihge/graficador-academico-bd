# Troubleshooting

Se completa a medida que aparecen problemas reales durante el desarrollo.

## Instalacion

- Si `npm install` falla por versiones "latest" incompatibles entre si,
  fijar manualmente la version conflictiva en `package.json` y documentar
  el porque en `docs/DECISIONS.md`.
